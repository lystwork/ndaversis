# Ndaversis Requirements
# Agentic Semantic Version Info System

# Core GUI Framework (Required)
PyQt6>=6.6.0

# AI Provider SDKs (Optional - for AI-powered features)
# The system works in offline mode without these
openai>=1.0.0

# Note: All other dependencies are from Python's standard library:
# argparse, ast, datetime, difflib, getpass, json, os, random, re, sys, time, typing
